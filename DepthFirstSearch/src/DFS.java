import java.util.Stack;


public class DFS {
	
	/**
	 * Step 1: Push the root node in the Stack
	 * Step 2: Loop until stack is empty
	 * Step 3: Peek the node of the stack
	 * Step 4: If node has unvisited child nodes, get the 
	 * child node, mark it as visited and push to stack
	 * Step 5: If node does not have any unvisited children,
	 * pop off the stack.
	 */
	public void dfs_search() {
		Stack s = new Stack();
	}

}
